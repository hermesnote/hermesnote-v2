export default function ModelTrainingPage() {
  return (
    <div style={{ padding: "48px clamp(20px, 4vw, 48px)" }}>
      <h1 style={{ fontFamily: "var(--font-display)", fontWeight: 500 }}>
        模型訓練
      </h1>
      <p style={{ color: "var(--ink-soft)" }}>骨架先立起來，內容之後補。</p>
    </div>
  );
}
