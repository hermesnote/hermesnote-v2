# 2026-08-17

## 討論內容

**重做目的與優先順序**
- 刻意練習 Python 後端架構分層優先，求職作品集是自然結果，不為了趕作品集犧牲架構品質
- 目標職位：嘉實資訊等量化/AI 相關

**現況評估**
- 舊站 `D:\hermesnote` 的 FastAPI 其實是主流寫法（routers + Pydantic + async SQLAlchemy），問題不在框架選擇，而在缺少 service/repository 分層，且 feature library、資料來源寫死在程式碼裡（見 `backend/routers/tasks.py`，572 行、參數硬編碼）
- 結論：後端維持 FastAPI，換分層不換框架；前端維持 React，這次刻意練習重點放後端

**保留範圍（概念，非程式碼）**
- 三階段 train/val/test 流程
- 量化回測整體概念
- 登入系統的必要性（重新設計）

**不保留**
- 舊站 API 設計、訓練紀錄、會議記錄資料
- 過度工程化 — 擴充性只用在真正需要展示設計能力的地方（feature library、資料來源）

**這次要展示的技術重點**
1. 後端分層架構（router/service/repository）
2. feature library、資料來源 config-driven，不寫死
3. TA-Lib、回測框架整合（vectorbt / backtrader / zipline-reloaded，待選）
4. 前端視覺與資訊架構重新設計
5. Portfolio 呈現方式（本專案 + 記帳 agent、研究成果等其他專案如何組織）

**文件與目錄結構決策**
- 參考舊站 `INDEX.md` + `research/record/index.md` 的模式（驗證過有效）
- 根目錄：`docs / frontend / backend / data` 同層（比照舊站 NAS 佈局，data 與 backend 同層方便相對路徑引用）
- `docs/index.md` 為文件目錄索引（不放根目錄，避免誤認為整站索引）
- `docs/record/`：`index.md` 摘要索引 + `archive/YYYY-MM-DD.md` 每日詳細記錄
- `docs/` 底下 spec.md / architecture.md / decisions.md 尚未建立，內容待討論定案

**Git**
- 建立 GitHub repo `hermesnote/hermesnote-v2`（Public）
- 本機 `git init` + 設定 remote
- 約定：之後每次有意義的開發階段完成，直接 commit + push，不用每次都問

## 待決問題

- [ ] `docs/spec.md` 內容與範圍
- [ ] `docs/architecture.md`：後端分層方式、前端目錄結構、部署拓樸細節
- [ ] `docs/decisions.md` 何時開始寫、格式
- [ ] UI/UX 設計流程：Hermes 用 Claude design 功能或網路範本產出視覺稿，我負責用程式碼落地
- [ ] Feature library / 資料來源 registry 的具體設計
