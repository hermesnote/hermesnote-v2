# 開發記錄索引

> 一行一則摘要，指向 `archive/` 底下當天的詳細記錄。平常不需要打開 archive，要追溯時再依日期查。

| 日期 | 摘要 | 詳細 |
|------|------|------|
| 2026-09-16 | **即時預覽／最終推論分批儲存全面整改 + 正式上線**：Batch 2（輪替式即時預覽、NOTIFY 8000-byte 上限修正、Loss/Accuracy 可讀性、窄視窗 CSS bug）→ 收尾輪（preview 寫入改有界佇列不擋訓練、歷史瀏覽換節點的 React 時序 bug）→ 機率向量保留（`output_type` discriminator）+ `inference.py` 真正串流化（`sliding_window_view`＋分批算分批寫）+ 前台 infer job 瀏覽 UI；建置修正（TS2339）+ `model_init.sql` 補防呆 ALTER 後，**已對正式 NAS DB 執行遷移並驗收**（兩張新表/欄位齊全、既有資料未受影響、兩支 API 回 200）；部署後抓到並修復真實推論 bug（asyncpg JSONB 未解碼），待 Hermes 重建 training 容器生效；另更新 `/hermes` 個人履歷頁內容 | [archive/2026-09-16-preview-inference-hardening.md](archive/2026-09-16-preview-inference-hardening.md) |
| 2026-08-17 | 專案啟動討論：確立重做目的（刻意練習優先於作品集）、保留範圍、目錄結構（docs/frontend/backend/data 同層，record 在 docs 底下）、建立 GitHub repo | [archive/2026-08-17.md](archive/2026-08-17.md) |
| 2026-08-18 | 定位修正為研究敘事型網站；前端骨架建立，Hero 區塊（logo+三分頁即時渲染面板）定稿，RWD 用 clamp() 修正 | [archive/2026-08-18.md](archive/2026-08-18.md) |
| 2026-09-02 | 版面改真頁面路由+深色主題；`/quant` 第一個垂直切片打通；後台 Google OAuth 登入+側欄外殼；TA-Lib 161 個指標登記表（含說明）；量化回測從單指標進化到多空雙向、可巢狀 AND/OR/加權的策略條件樹引擎 | [archive/2026-09-02.md](archive/2026-09-02.md) |
| 2026-09-06 | 回測改非同步 Job+DB 持久化架構（撐住 2.9M 根K棒）；`/quant` 頁面 XQ 式滾動軸/MA疊圖/交易定位/時區顯示 bug 多輪迭代；建立 `docs/agent-api/` 給 Agent 呼叫 API；NAS 部署規劃定案（archive 舊站、v2 部署到 `hermes/`、web API 與訓練容器分開） | [archive/2026-09-06.md](archive/2026-09-06.md) |
| 2026-09-08 | 訓練系統設計討論（流程事故：一開始誤在 `D:\hermesnote` V1 進行，已修正並歸位於此）；訓練架構收斂為「Feature Node/Model Node + DAG 圖」設計取代 V1 的 mode 字串分流；V1 三階段流程+data leakage 防範確認為設計原則參考、非程式碼沿用；即時交易與訓練訊號的範圍界定（三層分離） | [archive/2026-09-08.md](archive/2026-09-08.md) |
| 2026-09-09（續） | **訓練模組節點圖編輯器完成**：引入 React Flow，`/admin/model` 改成「+加訓練模組→存成卡片→拖線連接（模組接模組=融合）」；序列化邏輯測試通過（拉出的圖正確轉成 graph_spec）；真實 GPU 訓練跑出並修掉一個陣列對齊真實 bug（`-0` 切片陷阱）；補上 softmax 機率輸出選項（尚未實測）；本機裝了 CPU torch 方便以後除錯不用佔 NAS GPU | [archive/2026-09-08-training-plan.md](archive/2026-09-08-training-plan.md) |
| 2026-09-09 | **模型訓練前台/後台結構重做**：修正昨晚把建立訓練表單放前台的錯誤，改成 `/admin/model`（後台設定，比照 `/admin/quant`）+ `/model`（前台唯讀，讀 `?job=`）；本機測試意外觸發 NAS 真實訓練 worker 執行第一次真 LSTM 訓練，抓到並修掉 `fastapi` 依賴缺失的真實 bug（待下次 deploy 生效） | [archive/2026-09-08-training-plan.md](archive/2026-09-08-training-plan.md) |
| 2026-09-08（三） | **模型訓練系統：規劃 checklist + 全部落地完成（開工當晚）**：`model_training_progress` 表、Feature/Label/Model 三個可插拔 registry、DAG 執行引擎、worker 真訓練串接、WebSocket+LISTEN/NOTIFY 即時推送、`/model` 前端頁面（A設定/B K線/C即時曲線）全部本機測試通過（含真的開瀏覽器點按鈕驗證即時更新）；順便修掉 `fetch_ohlcv_rows` 未去重的既有 bug；**3 件事留給 Hermes 判斷**（正規化未接、三階段流程只做 train/val、epoch粒度vs window粒度落差）；**真正 GPU 訓練本身尚未實測，等 deploy 到 NAS 才是第一次驗證** | [archive/2026-09-08-training-plan.md](archive/2026-09-08-training-plan.md) |
| 2026-09-10 | **訓練系統 V2 重新設計：三方對齊 + 全部落地完成**：Hermes 打開 `/admin/model` 後發現特徵清單/Label-Target/Architecture 表單一連串設計問題，逐輪收斂後拉 Codex（研究端）三方對齊，產出完整組件契約（A時間戳基礎/B Target拆分+回歸/C訓練切分+洩漏防範/D模型產物存取+推論/E多模型對齊/F決策規則契約/G回測修正）+ 教授 Train/Val/Test 三階段協定的任務血緣機制（`phase`+`parent_job_id`），**全部 8 個里程碑（0～7）當晚實作完成並用真實 NAS DB + 真實 CUDA 訓練測試過**（含 Phase1→2→3 完整血緣鏈、模型融合依賴鏈存取+推論、holdout 邊界防護），過程中抓到並修掉多個真實 bug（LSTM 驗證階段 VRAM 未分批、`predict_proba` 序列化遺漏、推論階段 output_mode 未對齊導致特徵維度不符）；系統架構上契約打對，仍缺真正OOF，訓練結果暫不能當研究證據；**已 deploy 並實際操作驗證**。**第二輪**：Hermes 實機操作後回饋特徵分組缺失/split_strategy 選錯 Phase 的設計矛盾/模型參數太少，比對 V1＋教授課程教材補齊 LSTM/XGBoost 完整超參數（dense/bidirectional/l2/patience/class_weight/subsample/colsample_bytree）並補上正規化（MinMax train-only fit）跟 early stopping，過程中又抓到一個 XGBoost early stopping 推論階段 iteration_range 沒對齊的真實 bug 並修復；完整排程與測試記錄見 [archive/2026-09-10-training-v2-redesign-plan.md](archive/2026-09-10-training-v2-redesign-plan.md)，對齊過程見 `docs/temp/2026-09-10-three-party-design-alignment.md`；**第二輪尚未 deploy，等 Hermes 跑 `deploy.ps1`** | [archive/2026-09-10-training-v2-redesign-plan.md](archive/2026-09-10-training-v2-redesign-plan.md) |
| 2026-09-08（續） | **V2 正式取代 V1 上線**：三容器（frontend/backend/training）docker compose 化，deploy.ps1 定調「只部署不建容器」；GPU passthrough 除錯並驗證通過（RTX 5060 Ti 確認可用，compose GPU 語法需用 `deploy.resources.reservations.devices` 不是 `runtime: nvidia`）；`model_training_jobs` 表 + worker 骨架上線驗證通過；Failed to fetch/首頁跳轉/logo連結三個前端 bug 修復；`/hermes` 個人履歷頁上線（Codex 產出履歷內容維持靜態 iframe，header 改用真正的 `<Layout>` React 路由避免手刻樣式漂移；踩坑：nginx bind mount 內容變了不會自動重啟容器，deploy.ps1 補上強制 restart frontend） | [archive/2026-09-08-deploy.md](archive/2026-09-08-deploy.md) |
